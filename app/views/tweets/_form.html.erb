<h2>New Tweet!</h2>
<form  class="tweet-compose" data-tweets-ul="#feed">


  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>
  <strong class="chars-left">140 characters left</strong>
  <br>

    <div class="mentioned-users">
        <script type="text/template" >
        <div>
        <select name="tweet[mentioned_user_ids][]">
            <option></option>
          <% User.all.each do |user| %>
            <option value="<%= user.id %>"><%= user.username %></option>
          <% end %>
        </select>
        <a class="remove-mentioned-user" href="javascript:void(0)">Remove</a>
       </div>
      </script>
      <ul></ul>
      </div>

    <a href="javascript:void(0)" class="add-mentioned-user">Add mention</a>


  <br>

  <input type="Submit" value="Post Tweet!">
</form>
